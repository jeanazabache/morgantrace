# ─────────────────────────────────────────────────────────────────────────────
# MorganTrace — Dependencias para CI (tests únicamente)
# Versión ligera: sin XGBoost, LightGBM ni MLflow para que el pipeline sea rápido
# ─────────────────────────────────────────────────────────────────────────────

# API
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1

# Serialización (joblib se importa en main.py)
joblib==1.4.2
numpy==1.26.4

# HTTP client para TestClient de FastAPI
httpx==0.27.0

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
